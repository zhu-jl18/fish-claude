# claude-code-launcher

列出 `~/.claude` 下所有 `settings*` 文件，选择后以 `--dangerously-skip-permissions` 启动 Claude Code。

```powershell
. tools/claude-code-launcher/ccc.ps1
ccc
```

依赖 Windows PowerShell 的 `USERPROFILE` 和 `claude` 命令。
