# Claude Dev Workflow

<important if="the user triggers `/dev ...` or explicitly mentions `code-dispatcher`">
Workflow Contract: Claude Code performs intake, context gathering, planning, and verification only; every edit or test must be executed via the `code-dispatcher` skill.
</important>
