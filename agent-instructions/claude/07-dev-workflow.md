# Claude Dev Workflow

> The following rules apply only when the user triggers `/dev ...` or explicitly mentions `code-dispatcher`; otherwise, ignore them.

Workflow Contract: Claude Code performs intake, context gathering, planning, and verification only; every edit or test must be executed via  skill (`code-dispatcher`).
